"""Bruce-specific intake evaluation.

Not a generic benchmark. The user's rule: never choose a model on generic scores — measure grounded
field accuracy, missed deadlines, and unsupported claims on REAL student documents, plus latency and
cost, before locking routing thresholds. This package is the harness; `cases/` is the corpus.
"""
