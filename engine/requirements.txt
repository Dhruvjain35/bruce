# Bruce engine dependencies.
# Loose lower bounds for now; pin exact versions from the resolved lockfile after first install.

pydantic>=2.7          # typed data contracts — the grounding backbone
pydantic-ai-slim[openai]>=2.9   # agent framework: validated structured output (PromptedOutput), provider-neutral
httpx>=0.27            # async HTTP for scholarly APIs (OpenAlex, ROR) + PDF fetch
pdfplumber>=0.11       # extract corresponding-author emails from open-access PDFs
python-dotenv>=1.0     # load API keys from .env (never committed) at the entrypoint
fastapi>=0.115         # API service exposing the engine to clients
uvicorn[standard]>=0.30  # ASGI server for local/prod serving
sqlalchemy[asyncio]>=2.0  # ORM + async engine for Postgres persistence
asyncpg>=0.29          # async PostgreSQL driver
alembic>=1.13          # schema migrations (upgrade-from-empty + downgrade)
pyjwt[crypto]>=2.9     # verify IdP (Supabase/Apple) JWTs at the auth boundary
pillow>=11.1           # attachment normalization: decode/re-encode student photos + screenshots
pillow-heif>=1.0       # decode iPhone HEIC/HEIF (vision models reject HEIC) -> converted to JPEG

# dev
pytest>=8.0
pytest-asyncio>=0.23
google-cloud-tasks>=2.16   # Cloud Tasks dispatch (staging worker)
PyYAML>=6.0
